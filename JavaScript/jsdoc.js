/**
  合計値を算出する
  @param {number} left 左辺値
  @param {number} right 右辺値
  @return {number} 合計値
  @sample
    sum(1, 2): //=> 3
    sum(1); //=> NaN
*/
function sum(left, right) {
  return left + right;
}
